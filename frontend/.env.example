VITE_API_URL=http://localhost:8000/api

# Set to 'true' ONLY in development to enable mock API fallback when the backend is offline.
# Must be 'false' (or unset) in staging and production.
VITE_ENABLE_MOCK_API=false
